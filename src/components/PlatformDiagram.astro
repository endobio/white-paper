<div class="platform-diagram" role="img" aria-label="A measurement-to-decision biological intelligence loop">
  <div class="platform-diagram__node">
    <span class="platform-diagram__step">01</span>
    <strong>Molecular sensing</strong>
    <small>Continuous biological signals</small>
  </div>
  <span class="platform-diagram__arrow" aria-hidden="true">→</span>
  <div class="platform-diagram__node">
    <span class="platform-diagram__step">02</span>
    <strong>Signal interpretation</strong>
    <small>Contextual biological models</small>
  </div>
  <span class="platform-diagram__arrow" aria-hidden="true">→</span>
  <div class="platform-diagram__node">
    <span class="platform-diagram__step">03</span>
    <strong>Decision support</strong>
    <small>Earlier, targeted action</small>
  </div>
</div>

<style>
  .platform-diagram {
    display: grid;
    grid-template-columns: 1fr auto 1fr auto 1fr;
    align-items: stretch;
    gap: 0.75rem;
    font-family: var(--font-sans);
  }
  .platform-diagram__node {
    display: flex;
    min-height: 8rem;
    flex-direction: column;
    justify-content: space-between;
    padding: 1rem;
    border-top: 2px solid var(--color-accent);
    background: var(--color-surface);
  }
  .platform-diagram__step {
    color: var(--color-accent-dark);
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.12em;
  }
  .platform-diagram strong { font-size: 0.92rem; line-height: 1.3; }
  .platform-diagram small { color: var(--color-ink-muted); line-height: 1.4; }
  .platform-diagram__arrow {
    align-self: center;
    color: var(--color-accent-dark);
    font-size: 1.35rem;
  }
  @media (max-width: 680px) {
    .platform-diagram { grid-template-columns: 1fr; }
    .platform-diagram__arrow { transform: rotate(90deg); justify-self: center; }
  }
</style>
